# AGENTS.md

## 코딩 규칙

- Unity C# 스크립트를 작성, 수정, 리뷰하기 전 `docs/conventions/code-convention.md`를 참고한다.
- 커밋 전 변경된 Unity C# 파일은 `docs/conventions/pre-commit-checklist.md`를 참고한다.
- 문서 규칙과 기존 코드 패턴이 충돌하면 기존 코드의 일관성을 우선한다.
- 새 파일이나 새 기능에는 문서 규칙을 기본값으로 적용한다.

## 작업 규칙 및 금지 사항

- 불확실하면 불확실하다고 말한다.
- 추측한 내용과 확인된 사실을 섞지 않는다.
- 요청한 작업과 무관한 파일은 임의로 수정하지 않는다.
- 추측성 수정은 금지한다.
- 모호한 요구 사항이 있으면 작업 전 사용자에게 되물어본다. 그렇지 않으면 합리적으로 가정하고 진행하되, 어떤 가정을 했는지 반드시 밝힌다.
- 사용자 요청보다 더 좋은 제안이 있으면 작업 전에 제시하고, 사용자의 선택에 따라 진행한다.

## 작업 후 보고

- 코드 작성 후 컴파일 오류가 없는지 확인한다.
- 변경된 파일 목록을 요약한다.
- 사용자가 확인해야 할 내용을 요약한다.

## 말투 원칙

- 결론을 맨 앞에 쓴다.
- 짧고 명확한 문장을 쓴다. 한 문장에 하나의 내용만 담는다.
- 과장된 표현, 불필요한 감탄사, 영업 문구를 쓰지 않는다.
